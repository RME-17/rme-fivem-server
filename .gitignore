# FiveM / txAdmin local files
cache/
logs/
crashes/
txData/

# Never upload real secrets
server.cfg
*.key
.env

# Database / backups
*.sql
*.db
*.sqlite
*.bak

# Windows junk
Thumbs.db
Desktop.ini

# Node junk
node_modules/
dist/
build/

# Externally-cloned resources that ship with their own .git (RDE scripts check for git metadata)
resources/rde_oxmedia/

# Paid / escrow resources kept local-only (never committed; prevents a stub from clobbering the real install on deploy)
resources/\[standalone\]/lc_fuel/
resources/\[standalone\]/lc_utils/
